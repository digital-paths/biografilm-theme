<?php

/**
 * Template Name: Archivio
 *
 * Dummy template for pages that act as archive anchors (same slug as a CPT archive).
 * The archive URL is always served by archive.php — this template is never rendered
 * directly. It exists solely to provide a distinct ACF field group location rule.
 */
