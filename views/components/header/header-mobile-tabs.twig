<div class="border-t-stroke flex w-full border-t lg:hidden">
  <a
    href="{{ nav_urls.festival }}"
    class="text-tight flex flex-1 items-center justify-center border-b p-4 text-base transition-colors {{
    current_section == "festival"
      ? "bg-red border-b-red font-semibold text-white"
      : "bg-white hover:bg-white-hover active:bg-white-active border-b-stroke text-black"
    }}"
  >
    Festival
  </a>
  <a
    href="{{ nav_urls.industry }}"
    class="text-tight border-l-stroke flex flex-1 items-center justify-center border-b border-l p-4 text-base transition-colors {{
    current_section == "industry"
      ? "bg-red border-b-red font-semibold text-white"
      : "bg-white hover:bg-white-hover active:bg-white-active border-b-stroke text-black"
    }}"
  >
    Industry
  </a>
  <a
    href="{{ nav_urls.campus }}"
    class="text-tight border-l-stroke flex flex-1 items-center justify-center border-b border-l p-4 text-base transition-colors {{
    current_section == "campus"
      ? "bg-red border-b-red font-semibold text-white"
      : "bg-white hover:bg-white-hover active:bg-white-active border-b-stroke text-black"
    }}"
  >
    Campus
  </a>
</div>
